# วิธีการใช้งาน (How to Use)

คู่มือการใช้งานระบบ `YTRC-NestJS-Backend` ทั้งในโหมดการพัฒนา และการ Deploy ด้วย Docker

## 1. การเตรียมตัว (Prerequisites)

- **Node.js:** v20 หรือสูงกว่า (แนะนำ v24 ตามสภาพแวดล้อมปัจจุบัน)
- **Docker & Docker Compose** (สำหรับการ Deploy)
- **PostgreSQL Database**

## 2. ตั้งค่า Environment

คัดลอกไฟล์ `.env.example` เป็น `.env` และตั้งค่าข้อมูลจริง:
```bash
cp .env.example .env
```
*แก้ไข `DATABASE_URL` และ `JWT_SECRET` ให้ถูกต้อง*

## 3. การใช้งานในโหมดพัฒนา (Development)

หากต้องการรันบนเครื่องโดยสม่ำเสมอ:
```bash
# 1. ติดตั้ง dependencies
npm install --legacy-peer-deps

# 2. เริ่มรันระบบแบบ Watch mode
npm run start:dev
```
- API จะรันที่: `http://localhost:2530/api`
- Swagger UI: `http://localhost:2530/api/docs`

## 4. การใช้งานด้วย Docker (Production Ready) 🚀

วิธีนี้เป็นวิธีที่แนะนำสำหรับการ Deploy เพราะมาพร้อมกับ Nginx คั่นหน้า

### รันผ่าน Docker Compose
```bash
# Build และรัน Container ทั้งหมด (API + Nginx)
docker compose up -d --build
```

### การตรวจสอบสถานะ
```bash
# ดู Container ที่รันอยู่
docker ps

# ดู Log ของ API
docker compose logs -f api
```

### ช่องทางการเข้าถึงผ่าน Docker
- **API:** `http://localhost/api` (ผ่าน Nginx พอร์ต 80)
- **Uploads:** `http://localhost/uploads/`

## 5. การ Build โปรเจกต์ manually

หากต้องการตรวสอบว่าโค้ดไม่มี Error ก่อน Deploy:
```bash
npm run build
```
*ไฟล์จะถูกสร้างในโฟลเดอร์ `dist/`*

## ⚠️ หมายเหตุสำคัญ
- **Database Connection:** หากฐานข้อมูลอยู่บนเครื่อง Host (ไม่ใช่ Docker) ในไฟล์ `.env` ให้ใช้ host เป็น `host.docker.internal` แทน `localhost`
- **SSL:** การทำ HTTPS แนะนำให้ใช้ Nginx Proxy Manager หรือตั้งค่าเพิ่มเติมใน `nginx.conf`

---
*ดำเนินการโดย Antigravity AI*
